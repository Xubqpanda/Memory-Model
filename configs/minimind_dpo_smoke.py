model = dict(
    vocab_size=6400,
    block_size=768,
    n_layer=8,
    n_head=8,
    d_model=768,
    d_ff=3072,
    dropout=0.1,
    attention_type="mha",
    position_embedding_type="learned_absolute",
)

train = dict(
    data_dir="data/minimind/dpo_smoke",
    tokenizer="minimind",
    init_from="checkpoints/minimind_sft_60m/best.pt",
    reference_from="checkpoints/minimind_sft_60m/best.pt",
    out_dir="checkpoints/minimind_dpo_smoke",
    epochs=1,
    batch_size=2,
    gradient_accumulation_steps=1,
    beta=0.1,
    label_smoothing=0.0,
    average_log_probs=False,
    eval_interval=1,
    eval_batches=1,
    log_interval=1,
    learning_rate=5e-7,
    min_lr=5e-8,
    warmup_steps=1,
    weight_decay=0.0,
    grad_clip=1.0,
    dtype="bfloat16",
    compile=False,
    fused_optimizer=True,
    seed=1337,
    wandb_mode="disabled",
    wandb_project="Memory-Model",
    wandb_run_name="minimind-dpo-smoke",
)
